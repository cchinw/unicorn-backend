#!/usr/bin/env python
# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(name='unicorn-backend',
      version='1.0',
      packages=find_packages(),
      scripts=['manage.py'])
